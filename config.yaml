variables:
  regex: '<version.light[a-z-]+4j>\d\.\d\.\d</version.light[a-z-]+4j>'
paths:
- type: pom.xml
  path: light-*
  match: "{{ .regex }}"
  replace: "{{ .old }}"
  with: "{{ .new }}"
  must_include:
    - modelVersion
    - groupId
    - artifactId
